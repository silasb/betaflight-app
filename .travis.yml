sudo: false
language: go
go:
  - 1.10.x

os:
  - linux
  - osx

matrix:
  allow_failures:
    - go: tip
  fast_finish: true

install:

script:
  - go get -u golang.org/x/vgo
  - vgo get
  - diff -u <(echo -n) <(gofmt -d -s .)
  - vgo tool vet .
  - vgo test -v -race ./...

notifications:
  email:
    - silas.baronda@gmail.com